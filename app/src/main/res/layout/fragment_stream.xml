<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:weightSum="1"
    android:paddingTop="80dp">

    <ImageView android:layout_width="match_parent"
    android:layout_height="100dp"
    android:layout_marginTop="10dip"
        android:src="@drawable/ic_mic_black_24dp"
        android:tint="@color/colorPrimary"
        android:layout_weight="0.18"
        />

    <ProgressBar android:id="@+id/progressBar1"
        android:indeterminateOnly="false" android:progressDrawable="@android:drawable/progress_horizontal"
        android:indeterminateDrawable="@android:drawable/progress_indeterminate_horizontal"
        android:minHeight="20dip" android:maxHeight="20dip"
        android:layout_width="fill_parent" android:layout_height="wrap_content"
        android:layout_marginLeft="20dip" android:layout_marginRight="20dip"
        android:layout_marginTop="10dip"
        android:backgroundTint="@color/colorPrimary"></ProgressBar>
    <LinearLayout android:id="@+id/linearLayout1"
        android:layout_height="wrap_content" android:layout_width="match_parent"
        android:layout_marginTop="20dip" android:gravity="center">
        <ImageButton android:text="Play" android:id="@+id/buttonPlay"
            android:layout_width="50dp" android:layout_height="50dp"
            android:src="@drawable/ic_play_circle_filled_black_24dp"
            android:scaleType="fitCenter"
            android:tint="@color/colorPrimary"
            android:background="@null">
        </ImageButton>
        <ImageButton android:text="Stop" android:id="@+id/buttonStopPlay"
            android:layout_width="50dp" android:layout_height="50dp"
            android:src="@drawable/ic_pause_circle_filled_black_24dp"
            android:layout_marginLeft="10dp"
            android:background="@null"
            android:tint="@color/colorPrimary"
            android:scaleType="fitCenter"></ImageButton>
    </LinearLayout>
</LinearLayout>